<%= form_for(@smodule, html: {class: "form-horizontal"}) do |f| %>
  <% if @smodule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@smodule.errors.count, "error") %> prohibited this smodule from being saved:</h2>

      <ul>
      <% @smodule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, class: "control-label" %>
    <div class="controls"><%= f.text_field :name %></div>
  </div>
  <div class="controls">
    <%= f.submit 'Create Module', class: "btn btn-primary" %>
    <%= link_to 'Back', smodules_path, class: "btn" %>
  </div>
<% end %>
