<%= form_for(@site_category) do |f| %>
  <% if @site_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_category.errors.count, "error") %> prohibited this site_category from being saved:</h2>

      <ul>
      <% @site_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
